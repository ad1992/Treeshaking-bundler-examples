{
  "name": "treeshakable-repro",
  "version": "1.0.0",
  "description": "Trying out treeshaking across different bundlers",
  "module": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:esbuild": "node buildESM.js",
    "build:webpack": "node webpack.config.js",
    "build:all": "rm -rf dist && yarn build:esbuild && yarn build:webpack"
  },
  "author": "Aakansha Doshi",
  "license": "MIT",
  "devDependencies": {
    "@babel/core": "^7.23.7",
    "babel-loader": "^9.1.3",
    "esbuild": "^0.19.11",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "open-color": "^1.9.1",
    "react": "^18.2.0"
  }
}
